################################################################################
# mini‑ca  +  MariaDB  +  Nginx‑Proxy‑Manager
# -----------------------------------------------------------------------------#
# • `init`     – one‑shot bootstrap (root CA)               ⚙ run with profile
# • `minica`   – live domain‑watcher (long‑running)
# • `cli`      – on‑demand CA commands (one‑off container)
# • `cert-sync`– copies freshly‑issued certs into NPM (one‑off container)
# • `db`       – MariaDB for NPM
# • `npm`      – Nginx‑Proxy‑Manager
#
# All variable references come from the .env generated by *genenv.py*.
################################################################################
services:

  # ───────────────────────────── mini‑ca ──────────────────────────────
  init:
    image: minica:latest
    build:
      context: .
      dockerfile: docker/Dockerfile
    entrypoint: ["entry-init.sh"]
    user: "0"                  
    volumes:
      - minica-data:/data
    restart: "no"
    profiles: ["setup"]        

  minica:
    image: minica:latest       
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - minica-data:/data
    restart: unless-stopped

  cli:
    extends: minica            
    entrypoint: ["mini_ca.py"] 
    # 🚫  NO profile → always exists in the project graph
    volumes:
      - minica-data:/data
      - npm_data:/npm-data     

  # ─────────────────────── cert → NPM one‑shot copier ─────────────────
  cert-sync:
    image: alpine:latest
    container_name: cert-sync
    environment:
      - DOMAIN                 
    command: >-
      sh -ceu '
        SRC="/certs/certificates/${DOMAIN}"
        DST="/npm-data/custom_ssl/$(echo "$DOMAIN" | tr . _)"
        mkdir -p "$DST"
        cp "$SRC/${DOMAIN}.crt" "$DST/fullchain.pem"
        cp "$SRC/${DOMAIN}.key" "$DST/privkey.pem"
        echo "✅  copied cert to $DST"
      '
    volumes:
      - minica-data:/certs:ro  
      - npm_data:/npm-data     
    network_mode: none         
    depends_on:
      - cli                    
    profiles: ["sync"]         
    restart: "no"

  # ───────────────────────── MariaDB (back‑end) ───────────────────────
  db:
    image: jc21/mariadb-aria:latest
    container_name: npm_db
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${DB_MYSQL_NAME}
      - MYSQL_USER=${DB_MYSQL_USER}
      - MYSQL_PASSWORD=${NPM_DB_PASSWORD}
      - MARIADB_AUTO_UPGRADE=1
    volumes:
      - npm_mysql:/var/lib/mysql
    restart: unless-stopped

  # ─────────────── Nginx‑Proxy‑Manager (front‑end) ────────────────────
  npm:
    image: jc21/nginx-proxy-manager:latest
    container_name: ${NPM_CONTAINER_NAME}
    depends_on:
      - db
    environment:
      - DB_MYSQL_HOST=${DB_MYSQL_HOST}
      - DB_MYSQL_PORT=${DB_MYSQL_PORT}
      - DB_MYSQL_USER=${DB_MYSQL_USER}
      - DB_MYSQL_PASSWORD=${NPM_DB_PASSWORD}
      - DB_MYSQL_NAME=${DB_MYSQL_NAME}
      - INITIAL_ADMIN_EMAIL=${INITIAL_ADMIN_EMAIL}
      - INITIAL_ADMIN_PASSWORD=${NPM_INITIAL_PASSWORD}
    ports:
      - "${NPM_PORT}"      
      - "${NPM_UI_PORT}"   
      - "${NPM_S_PORTS}"   
    volumes:
      - npm_data:/data
      - npm_letsencrypt:/etc/letsencrypt
      - minica-data:/certs:ro      
    restart: unless-stopped

################################################################################
# named volumes – keep state between runs
################################################################################
volumes:
  minica-data:          
  npm_data:             
  npm_mysql:            
  npm_letsencrypt:      

